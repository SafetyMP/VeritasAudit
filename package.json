{
  "name": "veritasaudit-root",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "npm@10.8.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "ci": "bash scripts/ci-verify.sh",
    "bootstrap": "bash scripts/bootstrap.sh",
    "sandbox": "bash scripts/sandbox-execute.sh"
  },
  "devDependencies": {
    "turbo": "^2.0.4",
    "typescript": "^5.4.5"
  }
}
